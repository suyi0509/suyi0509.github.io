import React from 'react';
import { Space, Image, Row, Col } from 'antd';
import Link from "next/link"


export default function Home({ Component, pageProps }) {

    const pageMenu = [
        { label: '我的项目', linkto: '/project/largeFiles' },
        { label: '我的简历', linkto: '/about/cv' },
        { label: '关于我', linkto: '/about/me' }
    ]

    const skills = [
        { text: 'JavaScript', bgColor: 'bg-blue-500' },
        { text: 'TypeScript', bgColor: 'bg-pink-500' },
        { text: 'HTML5', bgColor: 'bg-cyan-500' },
        { text: 'Vue', bgColor: 'bg-purple-500' },
        { text: 'React', bgColor: 'bg-blue-500' },
        { text: 'Taro', bgColor: 'bg-sky-500' },
        { text: 'Element', bgColor: 'bg-purple-800' },
        { text: 'Antd', bgColor: 'bg-rose-500' }
    ]

    return (
        <div className='slide-up-animation cursor-[url(https://suyi0509.github.io/icon/cursor-click.cur),auto]'>
            <div className='text-center mt-12'>
                <Image
                    className="rounded-full !w-44 !h-44 m-6"
                    src="https://suyi0509.github.io/pic.jpeg"
                />
                <div className='text-3xl m-6'> Hello, My name is Sue </div>
                <div className='text-lg m-6'> Welcome to my homepage </div>
            </div>
            <div className='flex justify-between translate-y-3.5'>
                {pageMenu.map((item, index) => {
                    return <Link href={item.linkto}
                        key={index} className='w-1/3 text-center text-black h-40 rounded-lg bg-gray-100 m-2 p-4 translate-x-3px hover:scale-105 hover:bg-pink-100'>
                        <Image
                            className="rounded-full !w-16 !h-16"
                            src={`https://suyi0509.github.io/icon/icon${index + 1}.png`}
                            preview={false}
                        />
                        <div className='font-semibold mt-6'><span>{item.label}</span></div>
                    </Link>
                })}
            </div>
            <div className='text-center flex justify-center mt-16'>
                {skills.map((item) => <div className={`text-white ${item.bgColor} px-1 text-sm`} key={item.text}>{item.text}</div>)}
            </div>
        </div>

    );

}
